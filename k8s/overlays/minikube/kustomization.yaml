# Local (Minikube) deployment: images built straight into Minikube's Docker
# daemon as :local, Ingress host is flight-ai.local (see README/CLAUDE.md
# run instructions). No changes needed on top of the base.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
